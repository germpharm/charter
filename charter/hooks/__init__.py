"""Charter Hooks — always-on hashing for every interaction.

v3.1.1: The Zeroth Law. Every state-changing interaction produces a
signed hash chain entry. These hooks wire into git, Claude Code,
VS Code, and build systems to ensure the chain never goes silent.
"""
